# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'Jokes Box' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Jokes Box
  pod 'Siesta', '~> 1.0'
  pod 'Alamofire', '~> 4.3'
  pod 'Gloss', '~> 1.2'
  pod 'SwiftyJSON'

  target 'Jokes BoxTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Jokes BoxUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
